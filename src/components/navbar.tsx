// v12: Stubbed. The old top tab bar is replaced by the AppShell sidebar.
// Every page that still imports `<Navbar />` renders nothing — intentional.
// Safe to delete imports later; not required for the layout to work.
export default function Navbar() {
  return null;
}
